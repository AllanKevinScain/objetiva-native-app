import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    padding: 2,
  },
  text: {
    fontSize: 14,
    textAlign: "center",
    letterSpacing: 0.5,
  },
});
