import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    width: "100%",
    flexDirection: "row",
    justifyContent: "center",
    gap: 10,

    paddingHorizontal: 1,
  },
  containerBall: {
    borderRadius: 6,
    borderWidth: 1,
    padding: 2,
    width: 24,
    height: 24,
    marginTop: 8,
  },
});
